# Changelog

## Unreleased

- Add per-move tuning fields: `moveControlFactor`, `lockMovement` (Alpha/Beta tuned) ✅
- Add in-game Debug UI (`F1`) for live tuning of smoothing and movement control ✅
- Implement time-based renderer smoothing and smoothed renderer positioning ✅
- Add automated move config tests and `npm test` runner ✅
- Fix multiple runtime issues and improve combat feel (hitstop, pose blending, cancel windows) ✅

